@model IEnumerable<ISOWebApps.Models.ProblemKendaraan>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.JadwalTunda)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.JadwalTunda)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProblemKendaraanID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProblemKendaraanID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProblemKendaraanID })
        </td>
    </tr>
}

</table>
